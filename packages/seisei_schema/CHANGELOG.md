## 0.1.0-dev.1

- Add typed flat object fields for strings, integers, numbers, booleans,
  arrays, and optional values while preserving `requiredStringFields`.

## 0.1.0-dev.0

- Add initial object schema validation contracts.
