## 0.1.0-dev.0

- Add an experimental optional Tagflow adapter that maps a narrow
  `seisei_ui` block vocabulary into `TagflowDocument` values and fails fast on
  unsupported blocks, actions, duplicate IDs, and invalid props.
